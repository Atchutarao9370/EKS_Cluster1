apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: my-cluster
  region: ap-south-1

managedNodeGroups:
  - name: ng-1
    instanceTypes: ["t3.micro"]
    desiredCapacity: 2
    spot: true
    ssh:
      allow: true
